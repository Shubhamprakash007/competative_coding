//vertical order traversal

#include<bits/stdc++.h>
using namespace std;


class TreeNode {
    int data;
    TreeNode* left;
    TreeNode* right;
};

vector<vector<int> verticalTreversal(TreeNode* root){
        queue<vector<vector<int,int>> q;
        vector<vector<int,int>> res;
        if(root==NULL)
            return res;

}
